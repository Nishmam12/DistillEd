fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9
python-dotenv>=1.0

# Provider SDKs — google-genai and anthropic are only exercised once a key is
# configured (GEMINI_API_KEY / ANTHROPIC_API_KEY); openai is used both for the
# GPT tier and, pointed at OpenRouter's base_url, for the always-on Gemma
# cloud tier.
openai>=1.0
google-genai>=2.0
anthropic>=0.116

# Dev/test only
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
