// COMMITTED — this repo is PRIVATE, so a shared dev-only HuggingFace token
// lives here for the whole team instead of each dev pasting their own into
// Settings. If this repo is ever made public, revert to gitignoring this file
// (see dev_secrets.example.dart) and rotate the token immediately.
library;

/// Debug-only HuggingFace token fallback. `''` = none — use Settings instead.
const String kDevHuggingFaceToken = '';
